<section class="hero is-warning">
  <div class="hero-body">
    <div class="container has-text-centered">
      <h1 class="title">
        グループ詳細
      </h1>
    </div>
  </div>
</section>

<div class='col-md-8 offset-md-1 mt-4'>
      <table class='table table-hover table-inverse'>
        <thead>
          <tr>
            <th>グループ名</th>
            <th>紹介文</th>
            <th>メンバー</th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><%= @group.name %></td>
            <td><%= @group.introduction %></td>
            <td>
              <% @group.customers.each do |member| %>
                <%= member.name %><br>
              <% end %>
            </td>
            <td>
              <% if @group.owner_id == current_customer.id %>
                <%= link_to '編集', edit_group_path(@group), class: "btn btn-sm btn-success" %>
              <% elsif @group.customers.include?(current_customer) %>
                <%= link_to '退会', group_path(@group), method: :delete, class: "btn btn-sm btn-danger" %>
              <% else %>
                <%= link_to '参加', group_join_path(@group), class: "btn btn-sm btn-success" %>
              <% end %>
            </td>
            <td><%= link_to 'メール',group_new_mail_path(@group), class: "fas fa-paper-plane" %></td>
          </tr>
        </tbody>
      </table>
</div>